/**
 * Tipos compartilhados entre frontend e backend.
 *
 * Os tipos da API são gerados automaticamente a partir do OpenAPI
 * expostos pelo FastAPI. Rode `pnpm --filter @auditoria/shared-types generate`
 * com a API rodando em http://localhost:8000 para atualizar `api.ts`.
 *
 * Enums e utilitários que NÃO vêm do OpenAPI ficam aqui em arquivos próprios.
 */

// Quando `api.ts` for gerado em S3, descomentar:
// export type * from './api';

export {};
