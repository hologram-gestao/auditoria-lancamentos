{
  "name": "@auditoria/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos TypeScript compartilhados — gerados do OpenAPI da API.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "openapi-typescript http://localhost:8000/openapi.json -o src/api.ts",
    "type-check": "tsc --noEmit",
    "lint": "echo 'no lint'",
    "test": "echo 'no tests'"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "openapi-typescript": "^7.4.1"
  }
}
