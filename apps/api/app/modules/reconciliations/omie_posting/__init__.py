"""Lançamento de linhas da fatura no Omie (Sprint 7).

`keys` deriva o `cCodIntLanc` por-linha; `repository` guarda a intenção de
lançamento (dedup primária do ADL). A chamada à Omie e o endpoint são da
BACK 07.4 — aqui não há nada que fale HTTP.
"""
